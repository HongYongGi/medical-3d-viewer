app:
  name: "Medical 3D Viewer"
  host: "0.0.0.0"
  port: 8501

renderer:
  host: "localhost"
  port: 8080
  url: "http://localhost:8080"

paths:
  uploads: "data/uploads"
  results: "data/results"
  models: "data/models"

session:
  timeout_minutes: 60

nnunet:
  results_dir: ""
  raw_dir: ""
  preprocessed_dir: ""
  weight_dir: "${NNUNET_WEIGHT_DIR}"
  auto_scan: true
